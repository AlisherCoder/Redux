import React from "react";

// interface Props {
//    title: string;
//    price: number | null;
//    desc?: string;
//    children?: ReactNode;
//    btn?: ReactNode;
// }

const Footer = () => {
   return <div>Footer</div>;
};

export default React.memo(Footer);
